apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argo 
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: argo 
  project: argo 
  source:
    helm:
      valueFiles:
        - values-cd.yaml
    chart: argo-cd
    repoURL: https://argoproj.github.io/argo-helm
    targetRevision: 3.6.8
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
